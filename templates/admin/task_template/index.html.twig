{% extends 'base.html.twig' %}

{% block title %}Szablony Zadań{% endblock %}

{% block body %}
<div class="max-w-[1100px] mx-auto px-6 py-12 text-slate-900">
    <div class="bg-white border border-slate-200 rounded-2xl p-8 mb-8 shadow-sm">
        <div class="flex flex-col md:flex-row justify-between items-center gap-6">
            <div>
                <h1 class="text-3xl md:text-4xl font-extrabold text-slate-900 leading-none mb-2 italic">
                    Szablony Zadań
                </h1>
                <p class="text-slate-500 font-medium italic">Zarządzaj cyklicznymi i jednorazowymi zadaniami</p>
            </div>
            <a href="{{ path('admin_task_template_new') }}" 
               class="px-8 py-4 bg-orange-600 text-white font-bold rounded-xl hover:bg-orange-700 shadow-sm transition-colors uppercase text-sm tracking-wider">
                Nowy Szablon
            </a>
        </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {% for template in task_templates %}
            <div class="bg-white border border-slate-200 rounded-2xl p-6 shadow-sm hover:border-orange-200 transition-colors group">
                <!-- Header with Points -->
                <div class="flex justify-between items-start mb-6">
                    <div class="flex-1">
                        <h3 class="text-xl font-black text-slate-900 mb-3 italic group-hover:text-orange-600 transition-colors">{{ template.name }}</h3>
                        <div class="flex flex-wrap gap-2">
                            <span class="inline-flex items-center px-2 py-0.5 rounded bg-slate-100 text-[10px] font-bold text-slate-600 uppercase tracking-tighter">
                                Dzień {{ template.weekday }}
                            </span>
                            {% if template.priority == 'URGENT' %}
                                <span class="inline-flex items-center px-2 py-0.5 rounded bg-red-100 text-[10px] font-bold text-red-700 uppercase tracking-tighter">
                                    PILNE
                                </span>
                            {% else %}
                                <span class="inline-flex items-center px-2 py-0.5 rounded bg-blue-100 text-[10px] font-bold text-blue-700 uppercase tracking-tighter">
                                    {{ template.priority }}
                                </span>
                            {% endif %}
                        </div>
                    </div>
                    <div class="flex-shrink-0 ml-4">
                        <div class="h-12 w-12 bg-slate-900 rounded-lg flex items-center justify-center shadow-sm">
                            <span class="text-lg font-black text-white italic">{{ template.points }}</span>
                        </div>
                    </div>
                </div>

                <!-- Badges Row -->
                <div class="flex flex-wrap gap-2 mb-6">
                    {% if template.recurring %}
                        <span class="inline-flex items-center px-2 py-0.5 rounded border border-orange-200 text-[10px] font-bold text-orange-600 uppercase">
                            🔄 Cykliczne
                        </span>
                    {% else %}
                        <span class="inline-flex items-center px-2 py-0.5 rounded border border-slate-200 text-[10px] font-bold text-slate-500 uppercase">
                            📌 Jednorazowe
                        </span>
                    {% endif %}
                    
                    {% if template.active %}
                        <span class="inline-flex items-center px-2 py-0.5 rounded bg-orange-50 text-[10px] font-bold text-orange-700 uppercase">
                            ✓ Aktywne
                        </span>
                    {% else %}
                        <span class="inline-flex items-center px-2 py-0.5 rounded bg-slate-50 text-[10px] font-bold text-slate-400 uppercase">
                            ✗ Nieaktywne
                        </span>
                    {% endif %}

                    {% if template.defaultWorker %}
                        <span class="inline-flex items-center px-2 py-0.5 rounded bg-blue-50 text-[10px] font-bold text-blue-700 uppercase">
                            👤 {{ template.defaultWorker.shortName }}
                        </span>
                    {% endif %}
                </div>

                <!-- Actions -->
                <div class="pt-4 border-t border-slate-100">
                    <a href="{{ path('admin_task_template_edit', {'id': template.id}) }}" 
                       class="block w-full text-center px-4 py-2 border border-slate-200 text-slate-600 font-bold rounded-lg hover:bg-slate-50 transition-colors text-xs uppercase tracking-widest">
                        Edytuj
                    </a>
                </div>
            </div>
        {% else %}
            <div class="col-span-full">
                <div class="bg-white border border-slate-200 rounded-2xl p-24 shadow-sm text-center">
                    <p class="text-2xl text-slate-900 font-black italic mb-2 tracking-tighter underline decoration-orange-500 decoration-4">Brak szablonów zadań</p>
                    <p class="text-slate-500 font-medium mb-10 italic">Utwórz pierwszy szablon, aby rozpocząć zarządzanie zadaniami</p>
                    <a href="{{ path('admin_task_template_new') }}" 
                       class="inline-block px-8 py-4 bg-orange-600 text-white font-bold rounded-xl hover:bg-orange-700 shadow-sm transition-colors uppercase text-sm tracking-wider">
                        Utwórz Szablon
                    </a>
                </div>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
